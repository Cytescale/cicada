import React,{useEffect, useRef, useState} from "react";


const cluster:React.FC<any> = (props:any)=>{
     return(
          <div>
               base
          </div>
     )
}

export default cluster;